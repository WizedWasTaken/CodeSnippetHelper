/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "cdn.wamiz.fr", "i.pinimg.com"],
  },
};

export default nextConfig;
